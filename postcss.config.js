// postcss-loader单独写在postcss.config.js里
// PostCSS is a tool for transforming CSS with JS plugins. 
// These plugins can support variables and mixins, transpile future CSS syntax, inline images, and more.
module.exports = {
  plugins: {
    autoprefixer: {}
  }
}
